node_modules
.next
dist
.turbo
.env
.env.local
.env*.local
deliverables/*
!deliverables/.gitkeep
*.tsbuildinfo
.DS_Store

# Front Citari — dépôt Git autonome, synchronisé avec Lovable.
# Volontairement non versionné ici : c'est un checkout lié, pas un sous-dossier.
# Cloner avec :  git clone https://github.com/LuigiRevelli/sprint-voice-insight.git apps/citari
apps/citari/
